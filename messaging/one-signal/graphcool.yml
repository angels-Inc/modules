# GraphQL types
types: ./types.graphql

functions:
  hello:
    handler:
      code:
        src: ./code/hello.js
    type: resolver
    schema: ./code/hello.graphql

# Model/Relation permissions
permissions:
- operation: "*"


# Permanent Auth Token / Root Tokens
rootTokens: []
